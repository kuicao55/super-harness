---
description: "Write an implementation plan for an existing spec or set of requirements. Skips brainstorming. Includes scale assessment and optional cross-session milestone tracking."
---

Invoke the `harness:harness-entry` skill with context: this is a `/super-harness:plan` invocation.

Tell the user: "Starting harness plan-writing. I'll assess the project scope, set up cross-session tracking if needed, and produce a detailed implementation plan."

Then immediately read and follow the `harness:harness-entry` skill, passing the context that this is a `/super-harness:plan` invocation.
